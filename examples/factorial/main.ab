//! Factorial example.
//!
//! Demonstrates recursive computation.

/// Computes the factorial of `n` using recursion.
fn factorial(n: number): number {
  if n <= 1 {
    1
  } else {
    n * factorial(n - 1)
  }
}

/// Entry point that computes 5!.
fn run(): number {
  factorial(5)
}
