[package]
name = "ambient-ability"
version.workspace = true
edition.workspace = true
description = "Core types for authoring Ambient abilities"

[dependencies]
ambient-core.workspace = true
blake3.workspace = true
serde = { workspace = true, features = ["rc"] }
