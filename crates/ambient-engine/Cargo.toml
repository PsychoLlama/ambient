[package]
name = "ambient-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = { version = "1.5.5", features = ["serde"] }
serde = { version = "1.0", features = ["derive", "rc"] }
thiserror = "2.0.11"

[dev-dependencies]
serde_json = "1.0"
